# SQL Notes

Place optional schema and seed SQL files here if needed for submission:

- `schema.sql`
- `seed.sql`
- `report-queries.sql`

Current web app assumes the 10 required tables already exist in Supabase.
